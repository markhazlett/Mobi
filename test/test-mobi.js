var expect = chai.expect;

describe("Mobi", function () {
  describe("Initial App Structure", function () {
    it("Mobi should exist", function (done) {
      expect(Mobi).to.exist;
      done();
    });
  });
});